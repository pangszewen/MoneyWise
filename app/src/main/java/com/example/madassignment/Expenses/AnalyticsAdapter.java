package com.example.madassignment.Expenses;

import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

import com.example.madassignment.R;

import java.util.List;
/*
public class AnalyticsAdapter extends RecyclerView.Adapter<AnalyticsAdapter.AnalyticsViewHolder> {
    private List<String> itemList; // Replace with your data type

    public AnalyticsAdapter(List<String> itemList) {
        this.itemList = itemList;
    }

    @NonNull
    @Override
    public AnalyticsViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View itemView = LayoutInflater.from(parent.getContext()).inflate(R.layout.item_layout, parent, false);
        return new AnalyticsViewHolder(itemView);
    }

    @Override
    public void onBindViewHolder(@NonNull AnalyticsViewHolder holder, int position) {
        // Bind data to UI components in the item view
        String item = itemList.get(position);
        // Example: holder.textView.setText(item);
    }

    @Override
    public int getItemCount() {
        return itemList.size();
    }

    static class AnalyticsViewHolder extends RecyclerView.ViewHolder {
        // Define UI components for the item view
        // Example: TextView textView;

        public AnalyticsViewHolder(@NonNull View itemView) {
            super(itemView);
            // Initialize UI components here
            // Example: textView = itemView.findViewById(R.id.textView);
        }
    }
}
*/